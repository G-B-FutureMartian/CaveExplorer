from random import choice, randint

print("Welcome to cave explorer, a MUSH exploration game."
client = input("Client or host?")
if client === "Host" or "host":
      from flask import App, render_template
